"use client"

// Identical to candidate settings page — copy it exactly
// Only difference: this lives at /hr/settings instead of /candidate/settings
// Import paths and logic are all the same

export { default } from "@/app/candidate/settings/page"
